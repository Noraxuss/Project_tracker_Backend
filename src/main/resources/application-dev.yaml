spring:
  datasource:
    url: jdbc:mysql://localhost:3306/feedback_intern
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: test
    password: develop
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8080

logging:
  level:
    org:
      springframework:
        web: DEBUG